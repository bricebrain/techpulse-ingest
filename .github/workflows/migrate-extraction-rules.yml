name: Migrate Neon Extraction Rules

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type APPLY_EXTRACTION_RULES to add source extraction rules"
        required: true

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Validate confirmation
        run: |
          test "${{ github.event.inputs.confirm }}" = "APPLY_EXTRACTION_RULES"

      - name: Install migration dependencies
        run: pip install "psycopg2-binary>=2.9"

      - name: Apply extraction rules migration
        env:
          NEON_DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
        run: python -m pipeline.run_sql_file migrations/005_source_extraction_rules.sql
